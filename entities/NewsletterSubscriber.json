{
  "name": "NewsletterSubscriber",
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "description": "Subscriber email address"
    },
    "source": {
      "type": "string",
      "description": "Where they subscribed from (footer, contact, etc.)",
      "default": "footer"
    }
  },
  "required": [
    "email"
  ]
}